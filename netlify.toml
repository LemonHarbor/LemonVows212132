[build]
  command = "npm install && npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20.18.0"
  NEXT_PRIVATE_TARGET = "static"

# Remove the redirect that's causing issues with Next.js routing
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
